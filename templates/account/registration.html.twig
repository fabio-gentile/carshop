{% extends "base.html.twig" %}

{% block title %}Inscription au site{% endblock %}

{% block body %}
    <div class="container full mt-5">
        <h1>Inscription sur Carshop</h1>
        {{ form_start(myForm) }}

        {{ form_widget(myForm) }}
        <button type="submit" class='btn btn-success'>Confirmez l'inscription</button>
        {{ form_end(myForm) }}
    </div>
{% endblock %}